import { WhereStartWithPipe } from './where-start-with.pipe';

describe('WhereStartWithPipe', () => {
  it('create an instance', () => {
    const pipe = new WhereStartWithPipe();
    expect(pipe).toBeTruthy();
  });
});
